extern crate image;
extern crate tensorflow;

use std::io;
use crate::image_rec::*;
use crate::voice_rec::*;

// input from the peripherals
pub fn input () {
	// camera input will also be here, and will write
	// face data to a separate .txt or .yml file for
	// storage and reference
}

// outputting signals and such
pub fn output () {
	// not sure if this function actually has much
	// purpose other than it being included in the
	// code structure I'm familiar with... the functions
	// might output on their own though, not sure yet
}